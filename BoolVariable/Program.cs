bool isComplete = true;
bool complete = false;
//this can change false
isComplete = !isComplete;

Console.WriteLine(isComplete);

int age = 23;


if (age >= 18)
{
    Console.WriteLine("you are eligable");
}
else
{
    Console.WriteLine("You are not eligable to vote");
}